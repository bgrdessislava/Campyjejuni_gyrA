#Code that unzips gzip files in one go
unzip *.zip

#Slurm script that changes the amount of memory you can run
#SBATCH --mem=5GB

#github
git status

git add . 

git commit -m 'messege you write'

git push 




for file in *cat.nwk; do sbatch newick_concatination.slurm $file; done


git filter-branch --force --index-filter \
  'git rm --cached --ignore-unmatch Non_r/matrix_res_comparison.pkl' \
  --prune-empty --tag-name-filter cat -- --all
  